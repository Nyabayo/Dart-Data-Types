//list is the ordered collection of the objects
//list is like an arrat and is used to store multiple values without creating multiple variables
void main() {
  List<String> names = ["Ernest", "Elvis", "Magda"];
  print(
      "The first name in the list is:${names[0]}\n The third name in the list is: ${names[2]}\n All the names in the list: $names ");
}
